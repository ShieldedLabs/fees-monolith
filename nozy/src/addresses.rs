#[derive(Debug, Clone, PartialEq)]
pub enum NetworkType {
    Mainnet,
    Testnet,
}

pub struct AddressManager {
    
}

impl AddressManager {
    pub fn new() -> Self {
        Self {}
    }
} 