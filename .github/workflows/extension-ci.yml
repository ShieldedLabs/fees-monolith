name: extension-ci

on:
  pull_request:
    paths:
      - "browser-extension/**"
      - ".github/workflows/extension-ci.yml"
  push:
    branches: [master]
    paths:
      - "browser-extension/**"
      - ".github/workflows/extension-ci.yml"

jobs:
  wasm-core-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: browser-extension/wasm-core
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - name: Host compile check
        run: cargo check
      - name: WASM target compile check
        run: |
          export CARGO_BUILD_RUSTC="$(rustup which --toolchain stable rustc)"
          rustup run stable cargo check --target wasm32-unknown-unknown
      - name: Worker utility tests
        working-directory: .
        run: node --test browser-extension/background/tx-utils.test.mjs browser-extension/background/mobile-sync.test.mjs browser-extension/background/tx-lifecycle.test.mjs

  popup-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: browser-extension/wasm-core/popup
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: browser-extension/wasm-core/popup/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build
      - run: npm audit --audit-level=moderate

  package-smoke:
    runs-on: ubuntu-latest
    needs:
      - popup-quality
      - wasm-core-check
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: browser-extension/wasm-core/popup/package-lock.json
      - name: Build WASM pkg
        working-directory: browser-extension/wasm-core
        run: wasm-pack build --target web --out-dir ../wasm/pkg --release
      - name: Build popup
        working-directory: browser-extension/wasm-core/popup
        run: |
          npm ci
          npm run build
      - name: Compose extension (manifest paths)
        run: |
          mkdir -p dist/nozy-extension/wasm-core/popup
          cp browser-extension/manifest.json dist/nozy-extension/manifest.json
          cp -r browser-extension/background dist/nozy-extension/background
          rm -f dist/nozy-extension/background/*.test.mjs
          cp -r browser-extension/content dist/nozy-extension/content
          cp -r browser-extension/wasm dist/nozy-extension/wasm
          cp -r browser-extension/wasm-core/popup/dist dist/nozy-extension/wasm-core/popup/dist
      - name: Validate tree
        run: |
          test -f dist/nozy-extension/manifest.json
          test -f dist/nozy-extension/wasm-core/popup/dist/index.html
          test -f dist/nozy-extension/wasm/pkg/nozy_wasm_bg.wasm
      - uses: actions/upload-artifact@v4
        with:
          name: nozy-extension-smoke
          path: dist/nozy-extension
