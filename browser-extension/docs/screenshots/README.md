# Extension screenshots

**Placeholder PNGs** (`01-*.png` … `05-*.png`) are committed so [`../README.md`](../README.md) renders on GitHub without broken images. **Replace** them with real captures when you can (same filenames).

Suggested filenames / content:

| File | What to capture |
|------|------------------|
| `01-extensions-developer-mode.png` | `chrome://extensions` or `edge://extensions` with Developer mode enabled |
| `02-load-unpacked.png` | File picker or folder after **Load unpacked** |
| `03-popup-overview.png` | Popup open on **Home** with wallet unlocked |
| `04-companion-api-tab.png` | **API** tab: URLs + **Check API** result |
| `05-settings-rpc.png` | **Settings** showing RPC endpoint |

Keep width reasonable (e.g. 900–1200px) for GitHub rendering.
